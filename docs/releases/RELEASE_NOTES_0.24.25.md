# Xylune 0.24.25

## UI fixes

- Keeps provider setup and connection-editor scroll position stable when expandable content such as advanced headers is opened or closed.
- Preserves model-list scroll state while configuring a provider.
- Replaces the abrupt full-screen model picker with a Material bottom sheet that slides in from the bottom and animates away before the selected model is applied.
- Removes the redundant “On-device history • BYOK” label from the conversation sidebar to free up space.
