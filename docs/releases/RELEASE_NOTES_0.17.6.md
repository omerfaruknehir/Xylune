# Turp 0.17.6

Gradual chrome blur is now present immediately when enabled rather than starting only after the chat scrolls. A 16 dp minimum radius remains active at rest, then the blur and Mica-style tint overlay rise together using a gentler quintic curve. The top and composer fade regions are wider and their AGSL spatial falloff uses the same smoother curve.
